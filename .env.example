# Copy this file to .env and fill in your real key.
# Only required if you want to RE-RUN llm_augment.py.
# The repo already ships pre-computed augmentation parquets, so most users
# never need an API key.
OPENAI_API_KEY=sk-...
